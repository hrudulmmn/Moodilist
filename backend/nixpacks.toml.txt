[phases.setup]
nixPkgs = ["python310", "python310Packages.pip"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn -k uvicorn.workers.UvicornWorker backend:app --bind 0.0.0.0:$PORT"